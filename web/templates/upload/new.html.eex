<h2>New upload</h2>

<%= render "form.html", changeset: @changeset,
                        action: upload_path(@conn, :create) %>

<%= link "Back", to: upload_path(@conn, :index), class: "ui-button ui-button--back" %>
